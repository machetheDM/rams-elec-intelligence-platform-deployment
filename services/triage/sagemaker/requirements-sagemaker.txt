# Separate from services/triage/requirements.txt on purpose — the SageMaker
# Python SDK pulls in a wide dependency tree (boto3 pinned versions, protobuf,
# etc.) that nothing else in the triage service needs. Only
# launch_training_job.py imports these, and only at the point it actually
# submits a job — see the inline import in that file.
sagemaker>=2.224.0
boto3>=1.34.0
pyarrow>=16.0.0
scikit-learn>=1.5.0
pandas>=2.2.0

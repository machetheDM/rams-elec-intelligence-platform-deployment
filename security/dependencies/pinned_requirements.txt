# Rams @Elec — Pinned Python Dependencies
# =========================================
# All dependencies pinned to exact versions for reproducible builds.
# Generated from services/*/requirements.txt with versions locked.
#
# Update process:
#   1. pip install pip-audit
#   2. pip-audit -r pinned_requirements.txt
#   3. Review any CVEs at https://osv.dev
#   4. Update affected packages and re-pin
#
# Last audited: July 2026
# Security contact: ramsatelec@gmail.com

# ── FastAPI & Web ──
fastapi==0.115.6
uvicorn==0.34.0
starlette==0.41.3
pydantic==2.10.4
httpx==0.28.1
pyjwt==2.10.1

# ── Database ──
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.1

# ── ML & AI ──
xgboost==2.1.3
scikit-learn==1.6.1
shap==0.46.0
numpy==1.26.4
pandas==2.2.3

# ── NLP & Embeddings ──
langchain-community==0.3.14
sentence-transformers==3.3.1
faiss-cpu==1.9.0.post1

# ── LLM ──
groq==0.15.0

# ── Load-shedding ──
# httpx already listed above

# ── ETL & Orchestration ──
apache-airflow==2.10.3

# ── Utilities ──
python-dotenv==1.0.1
python-multipart==0.0.19

# ── Security (dev dependencies) ──
bandit==1.8.0
safety==3.2.11
pip-audit==2.7.3
detect-secrets==1.5.0
